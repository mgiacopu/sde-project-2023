version: "3.7"

services:
  process-centric-layer:
    build:
      context: ./telegram_bot
    volumes:
      - ./telegram_bot:/app
    command: main.py

  data-layers:
    build:
      context: ./data-layers
    ports:
      - "8083:80"
    command: run start
